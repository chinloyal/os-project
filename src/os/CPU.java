package os;

public class CPU {
	private final static int NUMBER_OF_CORES = 2; // 2 cpu cores
	private static int memory = 200 * NUMBER_OF_CORES; // 200MB of memory for each core
	
	public CPU(Queue readyQueue) {
		
	}
}
